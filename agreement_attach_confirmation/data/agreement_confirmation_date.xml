<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Sequences for purchase.order -->
        <record id="seq_agreement_confirmation" model="ir.sequence">
            <field name="name">Agreement Confirmation</field>
            <field name="code">agreement.attach.confirmation</field>
            <field name="prefix">CONF</field>
            <field name="padding">5</field>
            <field name="company_id" eval="False"/>
        </record>
    </data>
</odoo>
